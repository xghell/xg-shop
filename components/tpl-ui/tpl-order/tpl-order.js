import style from '@/common/style.js';
import {toPx} from '@/common/helper.js';

const config = {
	data() {
		return {
			arrowrightAndTrashIconSize: toPx(style.UNI_FONT_SIZE_BASE),
		};
	}
};

export default config;