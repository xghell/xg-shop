import style from '@/common/style.js';
import {toPx} from '@/common/helper.js';

const config = {
	data() {
		return {
			arrowrightIconSize: toPx(style.UNI_FONT_SIZE_SM),
		};
	}
};

export default config;